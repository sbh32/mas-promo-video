name: Render Video
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  render:
    name: Render Video
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Render Mas Promo Video
        run: npx remotion render src/index.ts MasPromo out/mas-promo-video.mp4

      - name: Render Whatsapp Promo Video
        run: npx remotion render src/index.ts WhatsappPromo out/whatsapp-promo-video.mp4

      - name: Upload Video Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Rendered-Videos
          path: out/*.mp4
